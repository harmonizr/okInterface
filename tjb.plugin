#!name=自定义间隔定时脚本
#!desc=面板设置几分钟执行一次

[Argument]
# 这里就是你截图里的输入框，直接在Loon控制面板修改
interval=input,"5",tag=执行间隔(分钟),desc=你想要几分钟执行一次
postBody=input,"{\"msg\":\"测试\"}",tag=POST内容,desc=发送的数据

[Script]
cron {interval} script-path=https://example.com.run.js,timeout = 300,tag=淘金币,argument=[{postBody}]